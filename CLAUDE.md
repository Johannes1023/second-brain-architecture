# CLAUDE.md

All rules for this vault live in `AGENTS.md`. Read it first and follow it.

Note: this pointer exists for tool-convention compatibility only. Rule 0 in `AGENTS.md` still applies: only ever use this vault with a model that runs on your own hardware. Pointing a cloud-backed tool at it breaks the design.
